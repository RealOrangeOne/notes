image: "node:lts-slim"

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - ~/.npm
    - ./.cache

pages:
  stage: deploy
  script:
    - npm ci --production
    - npm run release
  artifacts:
    paths:
      - public
  only:
    - master
